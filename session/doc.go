// Package session defines durable session, run, message, part, tool-call, and
// context-epoch contracts for eino-agent runtimes.
// SessionDiscoveryReader separately offers bounded workspace metadata discovery.
// Store and ExecutionStore provide bounded manual and run-fenced title mutation.
// Workspace selection and selected-session authorization remain host concerns.
package session
